<Window x:Class="Tiny.UI.MultipleKinectUI"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Server - Multiple-Kinect View"
        Height="700" Width="700">
    <Window.Resources>
        <SolidColorBrush x:Key="MediumGreyBrush" Color="#ff6e6e6e" />
        <SolidColorBrush x:Key="KinectPurpleBrush" Color="#ff52318f" />
        <SolidColorBrush x:Key="KinectBlueBrush" Color="#ff00BCF2" />
    </Window.Resources>

    <Grid Margin="10 10 10 10">
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Background="Gray" Orientation="Horizontal">
            <Label x:Name="Label"  Content="Multiple Kinects" FontWeight="Bold" FontSize="16" FontFamily="Microsoft Sans Serif" VerticalAlignment="Center"/>
        </StackPanel>

        <Viewbox x:Name="MultiKinectViewBox" Grid.Row="1" HorizontalAlignment="Center">
            <Image Source="{Binding MultipleKinectViewSource}" Stretch="UniformToFill" />
        </Viewbox>

    </Grid>
</Window>

